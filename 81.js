/* ---------- Document Object Model in JS ----------
DOM stands for Document Object Model

It's a tree-like structure created by the browser from an HTML page.Every HTML element is treated as an object in the DOM.

JavaScript can be used to:
    --> Access elements
    --> Modify content
    --> Add/remove classes or styles
    --> Handle user events
*/

console.log(document);
console.log(window);

